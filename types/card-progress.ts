export type CardProgress = {
  consecutiveHits: number;
  createdAt: string;
  deckId: string;
  cardId: string;
  totalAttempts: number;
  totalErrors: number;
  nextReview: string;
  interval: number;
  lastReviewed: string;
  updatedAt: string;
};
